// Placeholder — the 4-step pet profile wizard is built in Phase 2.
// Exists now because signup redirects here.
export default function OnboardingPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 p-6 text-center">
      <h1 className="text-xl font-bold">สร้างโปรไฟล์น้อง</h1>
      <p className="text-brown-muted">เร็วๆ นี้ — Phase 2</p>
    </main>
  );
}
